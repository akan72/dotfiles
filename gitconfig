[user]
	email = lexokan@gmail.com
	name = Alex Kan
	username = akan72

[core]
	editor = nvim
    pager = delta

[delta]
    # use n and N to move between diff sections
    navigate = true
    light = false
    side-by-side = true
    line-numbers = true

[diff]
    colorMoved = default

[interactive]
    diffFilter = delta --color-only
[add.interactive]
    useBuiltin = false # required for git 2.37.0

[merge]
    conflictstyle = diff3

[pager]
	branch = cat

[web]
	browser = google-chrome

